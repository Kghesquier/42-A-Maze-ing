*This project has been created as part
of the 42 curriculum by kghesqui, nirandje.*

# A-MAZE-ING

## DESCRIPTION :



## INSTRUCTIONS :

For compilation and execution there is a Makefile with the commands : `install, run, debug, clean, lint and lint-strict`. the command make compiles all the sources files using `python3`.

`install`: Install project dependencies using `pip`.

`run`: Execute the main script of the project (e.g., via Python interpreter).

`debug`: Run the main script in debug mode using Python’s built-in debugger `pdb`.

`clean`: Remove temporary files or caches (e.g., pycache, .mypy_cache) to
keep the project environment clean.

`lint`: Execute the commands `flake8 .` and `mypy . --warn-return-any
--warn-unused-ignores --ignore-missing-imports --disallow-untyped-defs
--check-untyped-defs`

`lint-strict` (optional): Execute the commands `flake8 .` and `mypy . --strict`

## RESOURCES :

### Here is some of the websites we visited for this project :
[PEP 257 docstring documentation](https://peps.python.org/pep-0257/)


Regarding AI, we didn't used it that much in the project itself but we asked claude code to review our program to be sure and to make some tests. we did our own test while coding but when it's finished we ask claude code. We also used claude to help me for the graphical things that can be present in the `README.md` file.

## STRUCTURE AND FORMAT

## MAZE GENERATION ALGORITHM

## REUSABLE CODE

## TEAM WORK !

| **date** |      **kghesqui**       |       **nirandje**       |
| -------- | ----------------------- | ------------------------ |
| 31/03    | read subject and doc    | right foo                |
| 01/04    | started the project     | right bar                |
| dates    | left baz                | right baz                |
